import type { NextConfig } from "next";
// import dotenv from "dotenv";

// dotenv.config();

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
  // env: {
  //   PORT: process.env.PORT || "5000", // 환경 변수 설정
  // },
};

export default nextConfig;
